apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-golang-v1
  labels:
    app: api-golang-v1
    service: backend
    framework: fiber
spec:
  replicas: 3
  selector:
    matchLabels:
      app: api-golang-v1
  template:
    metadata:
      labels:
        app: api-golang-v1
        service: backend
    spec:
      containers:
      - name: api-golang-v1
        image: yeelo/api-golang-v1:latest
        ports:
        - containerPort: 3005
          name: http
        env:
        - name: PORT
          value: "3005"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: url
        - name: REDIS_URL
          value: "redis://redis-service:6379"
        - name: KAFKA_BROKERS
          value: "kafka-service:9092"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: jwt-secrets
              key: secret
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: api-golang-v1-service
spec:
  selector:
    app: api-golang-v1
  ports:
  - protocol: TCP
    port: 3005
    targetPort: 3005
  type: ClusterIP
